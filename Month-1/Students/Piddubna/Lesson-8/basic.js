//First task
button[aria-label='Add an emoji'] svg
div[aria-label='Add an image'] svg
button[aria-label='Add a GIF'] svg
button[aria-label='Add a video'] svg
root-comment-box-start
div[data-hook='secondary-btn']
div[data-hook='primary-btn']

//Second task
$x('input[name="first-name"]')
$x('input[name="email"]')
$x('div.label')
$x('[data-testid="buttonElement"]')